module.exports.errorName = {
  conflict: "conflict"
};

module.exports.errorType = {
  conflict: {
    message: "the current request data has conflict (s) with the data in the database",
    statusCode: 409
  }
};
